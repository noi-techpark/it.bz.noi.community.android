<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NOICommunity" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/fill_color</item>
        <item name="colorPrimaryVariant">@color/fill_color</item>
        <item name="colorOnPrimary">@color/label_on_fill_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/label_on_fill_color</item>
        <item name="colorSecondaryVariant">@color/label_on_fill_color</item>
        <item name="colorOnSecondary">@color/fill_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/fill_color</item>
        <!-- Customize your theme here. -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>

    </style>

    <style name="ToolbarTextAppearence">
        <item name="android:textSize">26sp</item>
    </style>

    <style name="ToolbarTheme" parent="@style/Widget.MaterialComponents.ActionBar.Primary">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/label_on_fill_color</item>
    </style>

    <style name="Widget.App.Switch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <!--
        Switch checked: black
        Switch unchecked: medium gray
    -->
    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorSecondary">@color/fill_color</item>
        <item name="colorSurface">@color/switch_color</item>
        <item name="colorOnSurface">@color/switch_color</item>
    </style>

    <!--
        Switch checked: medium gray
        Switch unchecked: light gray
    -->
    <style name="ThemeOverlay.App.Switch2" parent="">
        <item name="colorSecondary">@color/switch_color</item>
        <item name="colorSurface">@color/label_on_fill_color</item>
        <item name="colorOnSurface">@color/switch_color</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_theme</item>
    </style>
</resources>
